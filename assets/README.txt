Place your Adhan audio files in this folder.

Required files:

Optional files:
- app_icon.ico : Custom icon displayed in the Windows system tray (falls back to a default if omitted).

Ensure the filenames match the configuration in config.json.
